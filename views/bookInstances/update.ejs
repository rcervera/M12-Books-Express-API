<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
  <%- include('../partials/header'); %>
</header>
<main>
	<div class="row mt-5">
        <div class="col">
            <h5>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                      <li class="breadcrumb-item">
                            <a href="/technicalbook">Technical Books</a></li>
                      <li class="breadcrumb-item active" aria-current="page">
                        <a href="/technicalbook/update/<%= instance.book._id %>">Edit</a>
                      </li>
                      <li class="breadcrumb-item active" aria-current="page">
                        <a href="/technicalbook/instance/<%= instance.book._id %>">Copies</a>
                      </li>
                      <li class="breadcrumb-item active" aria-current="page">
                        <strong>Edit</strong>
                      </li>
                    </ol>
                  </nav>
                
                </h5>
                <p class="lead"> 
                    Physical Book means a set of written, printed, illustrated, or blank sheets, made of ink, paper, parchment, or other materials
                </p>

                <dl class="row lead">
                    <dt class="col-sm-3">Title</dt>
                    <dd class="col-sm-9"><%= instance.book.title%>.</dd>

                    <dt class="col-sm-3">Authors</dt>
                    <dd class="col-sm-9">
                        <% for (var i =0; i < instance.book.author.length;  i++ ) { %>
                            <div>
                            <%= instance.book.author[i].firstname %> <%= instance.book.author[i].lastname %>
                        </div>
                    <% } %>
                    </dd>

                    <dt class="col-sm-3">Publisher</dt>
                    <dd class="col-sm-9"><%= instance.book.publisher.name%></dd>
                </dl>
                
         </div>   
         <div class="col">
            <form method="POST" action="/technicalbook/instance/update/<%= instance._id %>" class="row g-3">
                <div class="form-group">
                    <label for="imprintYear">Imprint year</label>
                    <input type="number" name="imprintYear" class="form-control" value="<%= instance.imprintYear %>" >
                </div>
		
		<div class="form-group">
			 <label for="status">Status</label>
			 <select type="text" name="status" class="form-control">
			 		
			 		<% if(instance.status == "Available") { %> 
			 			<option SELECTED>Available</option>
			 		<% } else { %>
			 			<option>Available</option>
			 		<% } %>

			 		<% if(instance.status == "No available") { %> 
			 			<option SELECTED>No available</option>
			 		<% } else { %>
			 			<option>No available</option>
			 		<% } %>

			 		<% if(instance.status == "Maintenance") { %> 
			 			<option SELECTED>Maintenance</option>
			 		<% } else { %>
			 			<option>Maintenance</option>
			 		<% } %>

           <% if(instance.status == "Lost") { %> 
            <option SELECTED>Lost</option>
          <% } else { %>
            <option>Lost</option>
          <% } %>

			 </select>			
		</div>
				
        <div class="col-12 float-end">
            <button type="submit" class="btn btn-dark">Save</button>
            <a class="btn btn-danger" href="/technicalbook/instance/delete/<%= instance._id %>" role="button">Delete</a>

        </div>
    </div>
	</form>

	<% if (typeof error !== "undefined") { %>
		<div class="alert alert-primary mt-2" role="alert">
		    <p>Form could not be submitted due to the following error:</p>
		    <p><%= error %></p>
		</div>
	<% } %>

	</div>
		
	</div>

</main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>