<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
  <%- include('../partials/header'); %>
</header>
<main>
	<div class="row mt-5">
        <div class="col">
            <h5>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                      <li class="breadcrumb-item">
                            <a href="/technicalbook">Technical Books</a></li>
                      <li class="breadcrumb-item active" aria-current="page">
                        <a href="/technicalbook/update/<%= book._id %>">Edit</a>
                      </li>
                      <li class="breadcrumb-item active" aria-current="page">
                        <a href="/technicalbook/instance/<%= book._id %>">Copies</a>
                      </li>
                      <li class="breadcrumb-item active" aria-current="page">
                        <strong>New</strong>
                      </li>
                    </ol>
                  </nav>
                
                </h5>
                <p class="lead"> 
                    Physical Book means a set of written, printed, illustrated, or blank sheets, made of ink, paper, parchment, or other materials
                </p>

                <dl class="row lead">
                    <dt class="col-sm-3">Title</dt>
                    <dd class="col-sm-9"><%= book.title%>.</dd>

                    <dt class="col-sm-3">Authors</dt>
                    <dd class="col-sm-9">
                        <% for (var i =0; i < book.author.length;  i++ ) { %>
                            <div>
                            <%= book.author[i].firstname %> <%= book.author[i].lastname %>
                        </div>
                    <% } %>
                    </dd>

                    <dt class="col-sm-3">Publisher</dt>
                    <dd class="col-sm-9"><%= book.publisher.name%></dd>
                </dl>
                
         </div>   
         <div class="col">  
            <form method="POST" action="/technicalbook/instance/create/<%= book._id %>" class="row g-3">
            <div class="form-group">
                <label for="imprintYear">Imprint year</label>
                <input type="number" name="imprintYear" class="form-control" placeholder="YYYY" min="1999" max="2020">
            
            </div>
            
            <div class="form-group">
                <label for="status">Status</label>
                <select type="text" name="status" class="form-control">
                        <option>Available</option>
                        <option>No available</option>
                        <option>Maintenance</option>
                        <option>Lost</option>
                </select>			
            </div>
                    
            <div class="col-12">
                <button type="submit" class="btn btn-dark float-end">Save</button>
            </div>
           

        </form>

        <% if (typeof error !== "undefined") { %>
            <div class="alert alert-primary mt-2" role="alert">
                <p>Form could not be submitted due to the following error:</p>
                <p><%= error %></p>
            </div>
        <% } %>

	</div>
		
	</div>

</main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>